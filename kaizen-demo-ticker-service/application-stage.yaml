spring:
  application:
    env: stage

demo:
  greeting: 'stage profile: Hello from Config Server'

logging:
  config: "classpath:logback-spring.xml"
  level:
    com.nvidia: INFO
    org.springframework.web: ERROR
