spring:
  application:
    env: prod

iex:
  greeting: 'prod profile: Hello from Config Server'

stock:
  greeting: 'prod profile: Hello from Config Server'

logging:
  config: "classpath:logback-spring.xml"
  level:
    com.nvidia: INFO
    org.springframework.web: ERROR
